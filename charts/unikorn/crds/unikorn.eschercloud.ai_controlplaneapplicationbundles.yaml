---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.1
  name: controlplaneapplicationbundles.unikorn.eschercloud.ai
spec:
  group: unikorn.eschercloud.ai
  names:
    categories:
    - unikorn
    kind: ControlPlaneApplicationBundle
    listKind: ControlPlaneApplicationBundleList
    plural: controlplaneapplicationbundles
    singular: controlplaneapplicationbundle
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.kind
      name: kind
      type: string
    - jsonPath: .spec.version
      name: version
      type: string
    - jsonPath: .spec.preview
      name: preview
      type: string
    - jsonPath: .spec.endOfLife
      name: end of life
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ControlPlaneApplicationBundle defines a bundle of applications
          related with a particular custom resource e.g. a ControlPlane has vcluster,
          cert-manager and cluster-api applications associated with it.  This forms
          the backbone of upgrades by allowing bundles to be switched out in control
          planes etc.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              applications:
                description: Applications is a list of application references for
                  the bundle.
                items:
                  properties:
                    name:
                      description: Name is the name of the application.  This must
                        match what is encoded into Unikorn's application management
                        engine.
                      type: string
                    reference:
                      description: Reference is a reference to the application definition.
                      properties:
                        kind:
                          description: Kind is the kind of resource we are referencing.
                          enum:
                          - HelmApplication
                          type: string
                        name:
                          description: Name is the name of the resource we are referencing.
                          type: string
                        version:
                          description: 'Version is the version of the application
                            within the application type. TODO: make mandatory.'
                          type: string
                      required:
                      - kind
                      - name
                      type: object
                  required:
                  - name
                  - reference
                  type: object
                type: array
              endOfLife:
                description: EndOfLife marks when this bundle should not be advertised
                  any more by Unikorn server.  It also provides a hint that users
                  should upgrade ahead of the deadline, or that a forced upgrade should
                  be triggered.
                format: date-time
                type: string
              preview:
                description: Preview indicates that this bundle is a preview and should
                  not be used by default.
                type: boolean
              version:
                description: Version is a semantic version of the bundle, must be
                  unique.
                type: string
            required:
            - version
            type: object
          status:
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
